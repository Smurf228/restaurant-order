<!DOCTYPE html>
<html lang="pl-PL">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="stylesheet" href="../../css/style.css">
    <!-- <script type="application/javascript" src="../../js/validationCommon.js"></script>
    <script type="application/javascript" src="../../js/validationClientForm.js"></script> -->


    <title> Restaurant DEMO</title>
</head>
<body>
<header>
    <h1>Restaurant</h1>
    <img src="../../images/main.jpg" alt="Restaurant">

</header>
<nav class="navbar">
    <ul>
        <li><a href="/" class="<%= navLocation == 'main' ? 'active' : ''%>"> Strona główna</a></li>
        <li><a href="/clients" class="<%= navLocation == 'client' ? 'active' : ''%>"> Client</a></li>
        <li><a href="/dishs" class="<%= navLocation == 'dish' ? 'active' : ''%>"> Dania</a></li>
        <li><a href="/oders" class="<%= navLocation == 'oder' ? 'active' : ''%>"> Zamówienie</a></li>
    </ul>
</nav>


<main>

<!--    <script type="application/javascript" src="/js/validationClientForm.js"></script>-->

    <h2><%= pageTitle %></h2>
    <form class="form" method="post" action="<%=formAction%>"  novalidate onsubmit="return validateForm();">
        <input type="hidden" name="_id" value="<%= client._id%>">

        <label for="firstName">Imie: <span class="symbol-required">*</span></label>
        <input class="<%= validationErrors.find(e => e.path.includes('firstName')) ? 'error-input' : ''%>" type="text" name="firstName" id="firstName" maxlength="15" minlength="5" placeholder="5-15 znakow" required value="<%=client.first_name %>"
                <%= (formMode == 'showDetails') ? 'disabled' : '' %>>
        <span id="errorFirstName" class="errors-text">
            <%if (validationErrors.find(e => e.path.includes('firstName'))) {%>
            <%= validationErrors.find(e => e.path.includes('firstName')).message %>
            <% } %>
        </span>

        <label for="lastName">Nazwisko: <span class="symbol-required">*</span></label>
        <input class="<%= validationErrors.find(e => e.path.includes('lastName')) ? 'error-input' : ''%>" type="text" name="lastName" id="lastName" maxlength="15" minlength="5" placeholder="5-15 znakow" required value="<%=client.last_name %>"
                <%= (formMode == 'showDetails') ? 'disabled' : '' %>>
        <span id="errorLastName" class="errors-text">
            <%if (validationErrors.find(e => e.path.includes('lastName'))) {%>
                <%= validationErrors.find(e => e.path.includes('lastName')).message %>
            <% } %>
        </span>

        <label for="email" >E-mail: <span class="symbol-required">*</span></label>
        <input class="<%= validationErrors.find(e => e.path.includes('email')) ? 'error-input' : ''%>" type="text" name="email" id="email" maxlength="30" minlength="2"  placeholder="np. nazwa@domen.pl" value="<%=client.emeil %>"
                <%= (formMode == 'showDetails') ? 'disable' : '' %>>
        <span id="errorEmail" class="errors-text">
            <%if (validationErrors.find(e => e.path.includes('email'))) {%>
                <%= validationErrors.find(e => e.path.includes('email')).message %>
            <% } %>
        </span>

        <label for="phoneNumber">Phone number:<span class="symbol-required">*</span></label>
        <input class="<%= validationErrors.find(e => e.path.includes('phoneNumber')) ? 'error-input' : ''%>" type="tel" name="phoneNumber" id="phoneNumber" maxlength="9" minlength="9" placeholder="9 cyfr" required value="<%=client.phone_number %>"
                <%= (formMode == 'showDetails') ? 'disable' : '' %>>
        <span id="errorPhoneNumber" class="errors-text">
            <%if (validationErrors.find(e => e.path.includes('phoneNumber'))) {%>
                <%= validationErrors.find(e => e.path.includes('phoneNumber')).message %>
            <% } %>
        </span>

        <% if (formMode == 'showDetails') {%>
            <div class="section-buttons">
                <a href="/clients/edit/<%= client._id%>" class="button-edit">Edytuj</a>
            </div>
        <% } %>

        <div class="form-buttons">
            <p id="errorsSummary" class="errors-text"></p>
            <% if (formMode != 'showDetails') { %>
            <input class="form-button-submit" type="submit" value="<%=btnLabel%>">
            <a class="form-button-cancel" href="/clients">Anuluj</a>
            <%} else { %>
                <a class="form-button-cancel" href="/clients">Powrót</a>
            <% } %>
        </div>
    </form>
</main>

<footer>
    Danylo Udod S20904
</footer>
</body>
</html>
